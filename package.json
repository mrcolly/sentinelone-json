{
  "name": "sentinelone-json-viewer",
  "version": "1.0.0",
  "description": "Chrome extension to view SentinelOne event logs in JSON format",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch",
    "lint": "npm run lint:js && npm run lint:css",
    "lint:js": "eslint src/content.js",
    "lint:css": "stylelint styles.css",
    "lint:fix": "eslint src/content.js --fix && stylelint styles.css --fix",
    "format": "prettier --write \"**/*.{js,css,json,md}\"",
    "validate": "npm run lint && echo '✅ All checks passed!'",
    "dev": "npm run build && npm run watch",
    "package": "npm run build && zip -r sentinelone-json.zip manifest.json styles.css build/ icons/ src/ README.md -x '*.DS_Store'"
  },
  "keywords": [
    "chrome-extension",
    "sentinelone",
    "json-viewer",
    "security",
    "logs"
  ],
  "author": "Alessio Colombo",
  "license": "MIT",
  "dependencies": {
    "cash-dom": "^8.1.5",
    "lodash-es": "^4.17.21"
  },
  "devDependencies": {
    "esbuild": "^0.19.11",
    "eslint": "^8.56.0",
    "prettier": "^3.1.1",
    "stylelint": "^16.1.0",
    "stylelint-config-standard": "^36.0.0"
  }
}
